import { Category } from "../types/Category"

export const categories: Category = {
    expensive: { title: 'Saída', color: '#e2143a', expense: true},
    income: { title: 'Entrada', color: 'green', expense: false},
}